#!/usr/bin/env bash
# agent-yamen — inject a message into a yamen's bus.
#
# Usage:
#   chuanwen --project <p> --to <role> [--from <role>] [--reply-to <id>] "body"
#   chuanwen -p <p> --to <role> < body.md
set -e

PROJECT=""
FROM="human"
TO=""
REPLY_TO=""
BODY=""

while [ $# -gt 0 ]; do
  case "$1" in
    -p|--project) PROJECT="$2"; shift 2 ;;
    --from)       FROM="$2"; shift 2 ;;
    --to)         TO="$2"; shift 2 ;;
    --reply-to)   REPLY_TO="$2"; shift 2 ;;
    -h|--help)
      cat <<EOF
chuanwen — inject a message into a yamen's bus.

Usage:
  chuanwen -p <project> --to <role> [--from <role>] [--reply-to <id>] "body"
  chuanwen -p <project> --to <role> < body.md

Examples:
  chuanwen -p example --to foreman "ship the v0.2 changelog"
  chuanwen -p example --from frontend --to backend "is /api/users returning enum or string?"

Default --from is "human" (you, the operator).
EOF
      exit 0
      ;;
    *) BODY="$*"; break ;;
  esac
done

[ -z "$PROJECT" ] && { echo "[chuanwen] --project (-p) <project> required" >&2; exit 1; }
[ -z "$TO" ]      && { echo "[chuanwen] --to <role> required" >&2; exit 1; }

ROOT="${YAMEN_ROOT:-$HOME/.agent-yamen}"
PROJ_ROOT="$ROOT/projects/$PROJECT"

[ -d "$PROJ_ROOT" ] || { echo "[chuanwen] project '$PROJECT' not found ($PROJ_ROOT/)" >&2; exit 1; }

if [ -z "$BODY" ]; then
  if [ -t 0 ]; then
    echo "[chuanwen] no body and no stdin. Pass \"...\" or pipe via stdin." >&2
    exit 1
  fi
  BODY=$(cat)
fi

OUTBOX="$PROJ_ROOT/bus/$FROM/outbox"
if [ ! -d "$OUTBOX" ]; then
  # auto-create outbox for special senders (human, upstream)
  if [ "$FROM" = "human" ] || [ "$FROM" = "upstream" ]; then
    mkdir -p "$OUTBOX" "$PROJ_ROOT/bus/$FROM/inbox" "$PROJ_ROOT/bus/$FROM/inbox/.done"
  else
    echo "[chuanwen] from='$FROM' has no outbox ($OUTBOX). Add this role in yamen.yaml first." >&2
    exit 1
  fi
fi

ID=$(uuidgen | tr '[:upper:]' '[:lower:]' | tr -d '-' | head -c 8)
TS=$(date -Iseconds)
TS_EPOCH=$(date +%s)
FNAME="${TS_EPOCH}-${ID}-${FROM}-to-${TO}.md"
PATH_OUT="$OUTBOX/$FNAME"

{
  echo "---"
  echo "from: $FROM"
  echo "to: $TO"
  echo "ts: $TS"
  echo "id: $ID"
  [ -n "$REPLY_TO" ] && echo "in_reply_to: $REPLY_TO"
  echo "---"
  echo
  echo "$BODY"
} >"$PATH_OUT"

echo "[chuanwen:$PROJECT] wrote $PATH_OUT"
echo "  $FROM -> $TO (id=$ID)"
echo "  watcher will route to bus/$TO/inbox/"
